# OpenRouter API key for LLM processing
OPENROUTERAI_API_KEY=sk_your_key_here

# Groq API key (if used)
GROQ_API_KEY=gsk_your_key_here

# Logging level: DEBUG (full data), INFO (metadata only), or OFF (no file logging)
# Leave unset or set to OFF for production to avoid writing log files
LOG_LEVEL=INFO